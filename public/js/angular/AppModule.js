(function(window, angular) {
  'use strict';

  angular
    .module('App', dependencies)
    .config(config);

  var dependencies = [];

  function config() {
    // To Do
  }
})(window, window.angular);
